1.  实现一个简单的 EmployeeRepository

        用 Map<Integer, Employee> 代替 List，实现 save, findById, deleteById, findAll.
        练习 Map 的 put, get, remove, values.
        为 EmployeeService 添加线程安全
2.  用 synchronized 或 ReentrantLock 包裹关键方法。

        观察多线程环境下的行为。  

3.  实现 Employee 的 Comparable 接口
    
        按 salary 排序。
        然后在 EmployeeService 里直接 Collections.sort(employees)。
        
4.  实现 EmployeeService 的 Stream 版本

        用 employees.stream().filter(...).collect(Collectors.toList()) 实现 findById、listBySalaryDesc 等。
          
5.  编写单元测试

        使用 JUnit (@Test) 验证增删改查功能。
        关注 断言 与 异常测试。